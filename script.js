const hub = {
    greenwind,
    javascript,
    webangency,
    etc
}

const btn_place02 = document.getElementById('place_02')
const btn_place03 = document.getElementById('place_03')
const btn_place04 = document.getElementById('place_04')
const btn_place05 = document.getElementById('place_05')

const btn_place01 = document.getElementById('place_01')


const hub_link = {
    place_01: btn_place01,

}
document.getElementById('place_06')

const btn_searchbar = document.getElementById('searchbar');


btn_searchbar.addEventListener('change', e => {

})

/**
 * Dans le code JavaScript que vous avez fourni, les deux points (:) sont utilisés pour définir une paire clé-valeur dans un objet. Voici une explication détaillée :
 *
 *     Objet JavaScript : En JavaScript, un objet est une collection de propriétés, où chaque propriété est définie par une clé (ou nom) et une valeur. Les clés sont généralement des chaînes de caractères, et les valeurs peuvent être de n'importe quel type (nombre, chaîne, objet, fonction, etc.).
 *
 *     Syntaxe : Dans votre code, hub_link est un objet qui contient une propriété appelée place_01. La syntaxe est la suivante :
 *
 * javascript
 *
 *     const hub_link = {
 *         place_01: btn_place01,
 *     }
 *
 *     Ici, place_01 est la clé, et btn_place01 est la valeur associée à cette clé. Cela signifie que hub_link.place_01 fera référence à l'élément DOM récupéré par document.getElementById('place_01').
 *
 * En résumé, les deux points dans ce contexte servent à associer une clé à une valeur dans un objet JavaScript.
 */
/